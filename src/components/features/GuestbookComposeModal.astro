---
---

<dialog id="guestbook-compose-modal" class="gb-compose-modal">
	<div class="gb-compose-overlay" data-close-compose></div>
	<div class="gb-compose-panel">
		<!-- 角标装饰 -->
		<div class="corner-mark top-left"></div>
		<div class="corner-mark top-right"></div>
		<div class="corner-mark bottom-left"></div>
		<div class="corner-mark bottom-right"></div>

		<!-- 头部 -->
		<div class="compose-header">
			<div class="header-bg"></div>
			<div class="header-content">
				<div class="author-info">
					<div class="author-avatar"></div>
					<span class="header-title">NEW MESSAGE</span>
				</div>
				<span class="header-tag">COMPOSE</span>
			</div>
		</div>

		<!-- 表单主体 -->
		<div class="compose-body">
			<div class="body-line"></div>
			<div class="body-content">
				<h3 class="form-title">发表留言</h3>
				<div class="title-underline"></div>

				<div class="form-fields">
					<div class="field-group">
						<label class="field-label" for="gb-author">
							<span class="label-marker">01</span>
							昵称 / ALIAS
						</label>
						<input
							id="gb-author"
							type="text"
							class="field-input"
							placeholder="输入你的昵称..."
							maxlength={30}
						/>
						<div class="field-hint">2-30个字符</div>
					</div>

					<div class="field-group">
						<label class="field-label" for="gb-content">
							<span class="label-marker">02</span>
							留言内容 / CONTENT
						</label>
						<textarea
							id="gb-content"
							class="field-textarea"
							placeholder="写下你想说的话..."
							rows="5"
							maxlength={500}
						></textarea>
						<div class="char-count" id="gb-char-count">0/500</div>
						<div class="field-hint">5-500个字符，不支持HTML和脚本</div>
					</div>
				</div>

				<div id="gb-error-message" class="error-message" style="display: none;">
					<span class="error-icon">
						<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
							<circle cx="12" cy="12" r="10"/>
							<line x1="12" y1="8" x2="12" y2="12"/>
							<line x1="12" y1="16" x2="12.01" y2="16"/>
						</svg>
					</span>
					<span class="error-text"></span>
					<button class="error-close" data-close-error>
						<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
							<line x1="18" y1="6" x2="6" y2="18"/>
							<line x1="6" y1="6" x2="18" y2="18"/>
						</svg>
					</button>
				</div>
			</div>
		</div>

		<!-- 底部操作 -->
		<div class="compose-footer">
			<div class="footer-bars">
				<div class="bar"></div>
				<div class="bar"></div>
				<div class="bar"></div>
				<div class="bar"></div>
				<div class="bar"></div>
			</div>
			<div class="footer-actions">
				<button class="btn-cancel" data-close-compose>
					ESC 取消
				</button>
				<button class="btn-submit" id="gb-submit" disabled>
					发送留言 >>
				</button>
			</div>
		</div>
	</div>
</dialog>

<script>
	function initGuestbookComposeModal() {
		const modal = document.getElementById('guestbook-compose-modal') as HTMLDialogElement | null;
		if (!modal) return;

		const authorInput = document.getElementById('gb-author') as HTMLInputElement | null;
		const contentInput = document.getElementById('gb-content') as HTMLTextAreaElement | null;
		const charCount = document.getElementById('gb-char-count');
		const submitBtn = document.getElementById('gb-submit') as HTMLButtonElement | null;

		if (!authorInput || !contentInput || !submitBtn) return;

		let isSubmitting = false;

		function updateSubmitState() {
			if (!submitBtn || !authorInput || !contentInput) return;
			submitBtn.disabled = !authorInput.value.trim() || !contentInput.value.trim() || isSubmitting;
		}

		// 字符计数
		contentInput.addEventListener('input', () => {
			if (charCount) charCount.textContent = contentInput.value.length + '/500';
			updateSubmitState();
		});
		authorInput.addEventListener('input', updateSubmitState);

		// 监听打开事件
		window.addEventListener('guestbook:open-compose', () => {
			modal.showModal();
			document.body.style.overflow = 'hidden';
			authorInput.value = '';
			contentInput.value = '';
			if (charCount) charCount.textContent = '0/500';
			updateSubmitState();
			setTimeout(() => authorInput.focus(), 100);
		});

		// 关闭
		function closeModal() {
			if (!modal) return;
			modal.close();
			document.body.style.overflow = '';
		}

		modal.querySelectorAll('[data-close-compose]').forEach(el => {
			el.addEventListener('click', closeModal);
		});

		modal.addEventListener('click', (e) => {
			if (e.target === modal) closeModal();
		});

		modal.addEventListener('close', () => {
			document.body.style.overflow = '';
		});

		modal.addEventListener('cancel', (e) => {
			e.preventDefault();
			closeModal();
		});

		const errorMessageEl = document.getElementById('gb-error-message');

		function showErrorMessage(message: string) {
			if (!errorMessageEl) return;
			const errorTextEl = errorMessageEl.querySelector('.error-text');
			if (errorTextEl) {
				errorTextEl.textContent = message;
			}
			errorMessageEl.style.display = 'flex';
			setTimeout(() => {
				errorMessageEl.classList.add('show');
			}, 10);
		}

		function hideErrorMessage() {
			if (!errorMessageEl) return;
			errorMessageEl.classList.remove('show');
			setTimeout(() => {
				errorMessageEl.style.display = 'none';
			}, 300);
		}

		if (errorMessageEl) {
			errorMessageEl.addEventListener('click', (e) => {
				if (e.target instanceof Element && e.target.closest('[data-close-error]')) {
					hideErrorMessage();
				}
			});
		}

		function getErrorMessage(status: number, errorText: string) {
			switch (status) {
				case 400:
					if (errorText.includes('prohibited')) {
						return '内容包含不安全的内容，请检查是否包含脚本或特殊标签';
					}
					if (errorText.includes('author')) {
						return '昵称长度必须在2-30个字符之间';
					}
					if (errorText.includes('content')) {
						if (errorText.includes('short')) {
							return '内容太短，至少需要5个字符';
						}
						return '内容长度必须在5-500个字符之间';
					}
					return '输入内容无效，请检查后重试';
				case 429:
					return '发送太频繁了，请稍后再试';
				case 500:
					return '服务器内部错误，请稍后重试';
				default:
					return `提交失败 (${status})，请稍后重试`;
			}
		}

		// 提交
		submitBtn.addEventListener('click', async () => {
			const author = authorInput.value.trim();
			const content = contentInput.value.trim();
			if (!author || !content || isSubmitting) return;

			hideErrorMessage();

			isSubmitting = true;
			submitBtn.textContent = '发送中...';
			submitBtn.disabled = true;

			try {
				const { postGuestbookMessage } = await import('@/utils/guestbook-api');
				const message = await postGuestbookMessage(author, content);
				window.dispatchEvent(new CustomEvent('guestbooknew', { detail: message }));
				closeModal();
			} catch (err) {
				console.error('Failed to post message:', err);
				let errorMsg = '❌ 提交失败，请稍后重试';
				if (err.response) {
					const status = err.response.status;
					const text = await err.response.text();
					let errorText = '';
					try {
						const json = JSON.parse(text);
						errorText = json.error || '';
					} catch {
						errorText = text;
					}
					errorMsg = getErrorMessage(status, errorText);
				} else if (err.message) {
					errorMsg = '❌ ' + err.message;
				}
				showErrorMessage(errorMsg);
			} finally {
				isSubmitting = false;
				submitBtn.textContent = '发送留言 >>';
				updateSubmitState();
			}
		});
	}

	if (document.readyState === 'loading') {
		document.addEventListener('DOMContentLoaded', initGuestbookComposeModal);
	} else {
		initGuestbookComposeModal();
	}
	document.addEventListener('astro:page-load', initGuestbookComposeModal);
	document.addEventListener('astro:after-swap', initGuestbookComposeModal);
</script>

<style>
	/* ========== 弹窗基础 ========== */
	.gb-compose-modal {
		position: fixed;
		inset: 0;
		z-index: 99999;
		display: none;
		align-items: center;
		justify-content: center;
		padding: 1rem;
		border: none;
		background: transparent;
		width: 100%;
		height: 100%;
		max-width: 100%;
		max-height: 100%;
		overflow: visible;
	}
	.gb-compose-modal[open] { display: flex; }
	.gb-compose-modal::backdrop { background: transparent; }

	.gb-compose-overlay {
		position: fixed;
		inset: 0;
		background: rgba(0, 0, 0, 0.5);
		backdrop-filter: blur(6px);
		z-index: -1;
		animation: gbFadeIn 0.2s ease-out;
	}
	:root.dark .gb-compose-overlay {
		background: rgba(0, 0, 0, 0.75);
	}

	.gb-compose-panel {
		position: relative;
		width: 90%;
		max-width: 520px;
		background: #ffffff;
		border: 2px solid #18181b;
		border-radius: 0.5rem;
		overflow: hidden;
		display: flex;
		flex-direction: column;
		animation: gbSlideIn 0.25s cubic-bezier(0.22, 0.68, 0.25, 1);
	}
	:root.dark .gb-compose-panel {
		background: #18181b;
		border-color: #52525b;
	}

	@keyframes gbFadeIn {
		from { opacity: 0; }
		to { opacity: 1; }
	}
	@keyframes gbSlideIn {
		from { opacity: 0; transform: scale(0.92) translateY(12px); }
		to { opacity: 1; transform: scale(1) translateY(0); }
	}

	/* 角标 */
	.corner-mark {
		position: absolute;
		width: 0.5rem;
		height: 0.5rem;
		border-radius: 50%;
		border: 1px solid #18181b;
		background: #f4f4f5;
		z-index: 20;
	}
	:root.dark .corner-mark {
		border-color: #52525b;
		background: #27272a;
	}
	.corner-mark::after {
		content: "";
		position: absolute;
		inset: 0;
		margin: auto;
		width: 60%;
		height: 1px;
		background: #18181b;
		transform: rotate(45deg);
	}
	:root.dark .corner-mark::after {
		background: #52525b;
	}
	.corner-mark.top-left { top: 0.5rem; left: 0.5rem; }
	.corner-mark.top-right { top: 0.5rem; right: 0.5rem; }
	.corner-mark.bottom-left { bottom: 0.5rem; left: 0.5rem; }
	.corner-mark.bottom-right { bottom: 0.5rem; right: 0.5rem; }

	/* 头部 */
	.compose-header {
		position: relative;
		height: 3.5rem;
		border-bottom: 2px solid #18181b;
		padding: 0 1.5rem;
		display: flex;
		align-items: center;
		justify-content: space-between;
		overflow: hidden;
	}
	:root.dark .compose-header {
		border-color: #52525b;
	}

	.header-bg {
		position: absolute;
		inset: 0;
		opacity: 0.06;
		pointer-events: none;
		background-image: repeating-linear-gradient(
			45deg,
			transparent,
			transparent 5px,
			#18181b 5px,
			#18181b 10px
		);
	}
	:root.dark .header-bg {
		background-image: repeating-linear-gradient(
			45deg,
			transparent,
			transparent 5px,
			#fafafa 5px,
			#fafafa 10px
		);
	}

	.header-content {
		position: relative;
		z-index: 10;
		display: flex;
		align-items: center;
		justify-content: space-between;
		width: 100%;
	}

	.author-info {
		display: flex;
		align-items: center;
		gap: 0.5rem;
	}

	.author-avatar {
		width: 0.75rem;
		height: 0.75rem;
		background: #18181b;
		border-radius: 0.125rem;
		animation: pulse 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;
	}
	:root.dark .author-avatar {
		background: #fafafa;
	}

	@keyframes pulse {
		0%, 100% { opacity: 1; }
		50% { opacity: 0.5; }
	}

	.header-title {
		font-size: 0.65rem;
		font-weight: 700;
		letter-spacing: 0.15em;
		text-transform: uppercase;
		color: #18181b;
	}
	:root.dark .header-title {
		color: #fafafa;
	}

	.header-tag {
		font-size: 0.6rem;
		font-family: ui-monospace, monospace;
		opacity: 0.6;
		color: #52525b;
		letter-spacing: 0.1em;
	}
	:root.dark .header-tag {
		color: #a1a1aa;
	}

	/* 表单主体 */
	.compose-body {
		position: relative;
		padding: 1.5rem;
		display: flex;
		overflow: hidden;
	}

	.body-line {
		position: absolute;
		left: 1rem;
		top: 1.5rem;
		bottom: 1.5rem;
		width: 1px;
		background: #d4d4d8;
	}
	:root.dark .body-line {
		background: #27272a;
	}

	.body-content {
		padding-left: 1rem;
		flex: 1;
		display: flex;
		flex-direction: column;
		overflow: hidden;
	}

	.source-tag {
		display: inline-block;
		padding: 0.125rem 0.5rem;
		font-size: 0.6rem;
		text-transform: uppercase;
		letter-spacing: 0.1em;
		border: 1px solid #06b6d4;
		color: #06b6d4;
		width: fit-content;
		margin-bottom: 0.75rem;
	}

	.form-title {
		font-size: 1.125rem;
		font-weight: 700;
		line-height: 1.3;
		margin-bottom: 0.75rem;
		color: #18181b;
		text-transform: uppercase;
		letter-spacing: -0.02em;
	}
	:root.dark .form-title {
		color: #fafafa;
	}

	.title-underline {
		width: 3rem;
		height: 1px;
		background: #18181b;
		margin-bottom: 1.25rem;
	}
	:root.dark .title-underline {
		background: #fafafa;
	}

	/* 表单字段 */
	.form-fields {
		display: flex;
		flex-direction: column;
		gap: 1.25rem;
	}

	.field-group {
		display: flex;
		flex-direction: column;
		gap: 0.5rem;
	}

	.field-label {
		font-size: 0.65rem;
		font-weight: 700;
		letter-spacing: 0.15em;
		text-transform: uppercase;
		color: #52525b;
		display: flex;
		align-items: center;
		gap: 0.5rem;
	}
	:root.dark .field-label {
		color: #a1a1aa;
	}

	.label-marker {
		font-family: ui-monospace, monospace;
		font-size: 0.6rem;
		color: #06b6d4;
		opacity: 0.8;
	}

	.field-input,
	.field-textarea {
		width: 100%;
		background: transparent;
		border: 1px solid #d4d4d8;
		border-radius: 0.25rem;
		padding: 0.625rem 0.75rem;
		font-size: 0.85rem;
		font-family: ui-monospace, monospace;
		color: #18181b;
		outline: none;
		transition: border-color 0.2s;
		resize: none;
	}
	:root.dark .field-input,
	:root.dark .field-textarea {
		border-color: #27272a;
		color: #fafafa;
	}
	.field-input:focus,
	.field-textarea:focus {
		border-color: #06b6d4;
	}
	.field-input::placeholder,
	.field-textarea::placeholder {
		color: #52525b;
		opacity: 0.5;
	}
	:root.dark .field-input::placeholder,
	:root.dark .field-textarea::placeholder {
		color: #a1a1aa;
	}

	.char-count {
		font-size: 0.6rem;
		font-family: ui-monospace, monospace;
		color: #52525b;
		opacity: 0.6;
		text-align: right;
	}
	:root.dark .char-count {
		color: #a1a1aa;
	}

	.field-hint {
		font-size: 0.55rem;
		color: #71717a;
		opacity: 0.7;
		margin-top: 0.25rem;
	}
	:root.dark .field-hint {
		color: #52525b;
	}

	.error-message {
		position: relative;
		display: flex;
		align-items: center;
		gap: 0.75rem;
		padding: 0.75rem 1rem;
		background: #fef2f2;
		border: 1px solid #fecaca;
		border-radius: 0.375rem;
		margin-top: 1rem;
		transform: translateY(-10px);
		opacity: 0;
		transition: opacity 0.3s ease, transform 0.3s ease;
	}
	:root.dark .error-message {
		background: #450a0a;
		border-color: #7f1d1d;
	}
	.error-message.show {
		transform: translateY(0);
		opacity: 1;
	}
	.error-icon {
		display: flex;
		align-items: center;
		justify-content: center;
		width: 1.5rem;
		height: 1.5rem;
		background: #ef4444;
		color: white;
		border-radius: 50%;
		flex-shrink: 0;
	}
	.error-icon svg {
		width: 0.875rem;
		height: 0.875rem;
		fill: currentColor;
	}
	.error-text {
		flex: 1;
		font-size: 0.75rem;
		color: #991b1b;
		line-height: 1.5;
	}
	:root.dark .error-text {
		color: #fca5a5;
	}
	.error-close {
		display: flex;
		align-items: center;
		justify-content: center;
		background: none;
		border: none;
		color: #9ca3af;
		cursor: pointer;
		padding: 0.25rem;
		line-height: 1;
		transition: color 0.2s;
	}
	.error-close svg {
		width: 0.875rem;
		height: 0.875rem;
	}
	.error-close:hover {
		color: #ef4444;
	}

	/* 底部 */
	.compose-footer {
		height: 2.5rem;
		border-top: 2px solid #18181b;
		display: flex;
		align-items: center;
		justify-content: space-between;
		padding: 0 1rem;
		background: #18181b;
	}
	:root.dark .compose-footer {
		border-color: #52525b;
		background: #fafafa;
	}

	.footer-bars {
		display: flex;
		gap: 0.25rem;
	}

	.bar {
		width: 0.25rem;
		height: 1rem;
		background: #ffffff;
		opacity: 0.4;
	}
	:root.dark .bar {
		background: #000000;
	}

	.footer-actions {
		display: flex;
		align-items: center;
		gap: 0.75rem;
	}

	.btn-cancel,
	.btn-submit {
		font-size: 0.65rem;
		font-weight: 700;
		text-transform: uppercase;
		letter-spacing: 0.1em;
		border: none;
		cursor: pointer;
		padding: 0.25rem 0.5rem;
		transition: opacity 0.2s, filter 0.2s;
		background: transparent;
	}

	.btn-cancel {
		color: #ffffff;
		opacity: 0.5;
	}
	:root.dark .btn-cancel {
		color: #000000;
	}
	.btn-cancel:hover {
		opacity: 0.8;
	}

	.btn-submit {
		color: #06b6d4;
		font-family: ui-monospace, monospace;
	}
	.btn-submit:hover:not(:disabled) {
		filter: brightness(1.2);
	}
	.btn-submit:disabled {
		opacity: 0.3;
		cursor: not-allowed;
	}

	/* 响应式 */
	@media (max-width: 640px) {
		.gb-compose-panel {
			max-width: 100%;
			width: 95%;
		}
		.compose-body {
			padding: 1.25rem;
		}
		.compose-header {
			padding: 0 1.25rem;
		}
	}
</style>
