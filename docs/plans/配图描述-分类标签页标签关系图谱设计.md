# 配图描述 — 分类标签页标签关系图谱设计

## 配图 1

**文档位置**：页面结构章节，说明 `categories.astro`、`content-utils.ts`、`TagGraph.astro` 和 ECharts 的关系。  
**内容描述**：展示文章内容集合进入 `getTagGraphData()`，生成 `nodes` 和 `links`，再传入 `TagGraph.astro` 渲染关系图；用户点击节点后读取节点内文章列表并打开分页弹窗。
